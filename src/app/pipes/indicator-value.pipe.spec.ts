import { IndicatorValuePipe } from './indicator-value.pipe';

describe('IndicatorValuePipe', () => {
  it('create an instance', () => {
    const pipe = new IndicatorValuePipe();
    expect(pipe).toBeTruthy();
  });
});
